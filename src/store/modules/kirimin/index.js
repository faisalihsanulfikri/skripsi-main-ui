import Vue from "vue";

import * as TYPES from "./types";

const namespaced = true;

const state = {};

const mutations = {};

const actions = {
  initialize({ dispatch }) {}
};

export default {
  namespaced,
  state,
  mutations,
  actions
};
